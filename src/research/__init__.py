"""research: Measurement instruments, not pipeline. Nothing here runs in a
scheduled chain except the one off probe, and nothing downstream reads
their output. They exist to answer questions with numbers.
"""
