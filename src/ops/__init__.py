"""ops: Whether the machine itself is running correctly: the trading day
guard, the per step status record every scheduled job writes, and the
watchdog that reads all three sources of truth back.
"""
